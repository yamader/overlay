--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -123,7 +123,7 @@ if (BOOST_HTTP_IS_ROOT)
         set(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} "${BOOST_SRC_DIR}/tools/cmake/include")
     else ()
         # From Boost Package
-        find_package(Boost REQUIRED COMPONENTS filesystem json url)
+        find_package(Boost REQUIRED COMPONENTS capy filesystem json url)
         foreach (BOOST_INCLUDE_LIBRARY ${BOOST_INCLUDE_LIBRARIES})
             if (NOT TARGET Boost::${BOOST_INCLUDE_LIBRARY})
                 add_library(Boost::${BOOST_INCLUDE_LIBRARY} ALIAS Boost::headers)
@@ -149,7 +149,7 @@ source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR}/src PREFIX "src" FILES ${BOOST_HTT
 
 function(boost_http_setup_properties target)
     target_compile_features(${target} PUBLIC cxx_constexpr)
-    target_include_directories(${target} PUBLIC "${PROJECT_SOURCE_DIR}/include")
+    target_include_directories(${target} PUBLIC $<BUILD_INTERFACE:${PROJECT_SOURCE_DIR}/include>)
     target_include_directories(${target} PRIVATE "${PROJECT_SOURCE_DIR}")
     target_link_libraries(${target} PUBLIC ${BOOST_HTTP_DEPENDENCIES})
     target_compile_definitions(${target} PUBLIC BOOST_HTTP_NO_LIB)
@@ -173,6 +173,7 @@ endif ()
 add_library(boost_http include/boost/http.hpp build/Jamfile ${BOOST_HTTP_HEADERS} ${BOOST_HTTP_SOURCES})
 add_library(Boost::http ALIAS boost_http)
 boost_http_setup_properties(boost_http)
+set(BOOST_HTTP_INSTALL_TARGETS boost_http)
 
 # bcrypt requires platform-specific libraries
 if (WIN32)
@@ -195,6 +196,7 @@ if (ZLIB_FOUND)
     target_link_libraries(boost_http_zlib PRIVATE ZLIB::ZLIB)
     target_compile_definitions(boost_http_zlib PUBLIC BOOST_HTTP_HAS_ZLIB)
     target_compile_definitions(boost_http_zlib PRIVATE BOOST_HTTP_SOURCE)
+    list(APPEND BOOST_HTTP_INSTALL_TARGETS boost_http_zlib)
 endif ()
 
 # Brotli
@@ -212,8 +214,41 @@ if (Brotli_FOUND)
     target_link_libraries(boost_http_brotli PRIVATE Brotli::common Brotli::decoder Brotli::encoder)
     target_compile_definitions(boost_http_brotli PUBLIC BOOST_HTTP_HAS_BROTLI)
     target_compile_definitions(boost_http_brotli PRIVATE BOOST_HTTP_SOURCE)
+    list(APPEND BOOST_HTTP_INSTALL_TARGETS boost_http_brotli)
 endif ()
 
+include(GNUInstallDirs)
+include(CMakePackageConfigHelpers)
+set(BOOST_HTTP_INSTALL_CMAKEDIR ${CMAKE_INSTALL_LIBDIR}/cmake/boost_http)
+foreach (TARGET ${BOOST_HTTP_INSTALL_TARGETS})
+    string(REGEX REPLACE "^boost_" "" EXPORT_NAME ${TARGET})
+    set_target_properties(${TARGET} PROPERTIES EXPORT_NAME ${EXPORT_NAME})
+    target_include_directories(${TARGET} PUBLIC $<INSTALL_INTERFACE:${CMAKE_INSTALL_INCLUDEDIR}>)
+endforeach ()
+install(TARGETS ${BOOST_HTTP_INSTALL_TARGETS}
+    EXPORT boost_http-targets
+    ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
+    LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
+    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR})
+install(DIRECTORY include/
+    DESTINATION ${CMAKE_INSTALL_INCLUDEDIR})
+install(EXPORT boost_http-targets
+    NAMESPACE Boost::
+    DESTINATION ${BOOST_HTTP_INSTALL_CMAKEDIR})
+configure_package_config_file(
+    cmake/boost_http-config.cmake.in
+    ${CMAKE_CURRENT_BINARY_DIR}/boost_http-config.cmake
+    INSTALL_DESTINATION ${BOOST_HTTP_INSTALL_CMAKEDIR})
+write_basic_package_version_file(
+    ${CMAKE_CURRENT_BINARY_DIR}/boost_http-config-version.cmake
+    VERSION ${Boost_VERSION}
+    COMPATIBILITY SameMajorVersion)
+install(FILES
+    ${CMAKE_CURRENT_BINARY_DIR}/boost_http-config.cmake
+    ${CMAKE_CURRENT_BINARY_DIR}/boost_http-config-version.cmake
+    ${CMAKE_CURRENT_SOURCE_DIR}/cmake/FindBrotli.cmake
+    DESTINATION ${BOOST_HTTP_INSTALL_CMAKEDIR})
+
 #-------------------------------------------------
 #
 # Tests
--- /dev/null
+++ b/cmake/boost_http-config.cmake.in
@@ -0,0 +1,22 @@
+@PACKAGE_INIT@
+
+include(CMakeFindDependencyMacro)
+
+find_dependency(Threads)
+find_dependency(boost_headers)
+find_dependency(boost_capy)
+find_dependency(boost_json)
+find_dependency(boost_url)
+
+if(@ZLIB_FOUND@)
+    find_dependency(ZLIB)
+endif()
+
+if(@Brotli_FOUND@)
+    list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}")
+    find_dependency(Brotli)
+endif()
+
+include("${CMAKE_CURRENT_LIST_DIR}/boost_http-targets.cmake")
+
+check_required_components(boost_http)
