--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -123,7 +123,7 @@ if (BOOST_BEAST2_IS_ROOT)
         set(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} "${BOOST_SRC_DIR}/tools/cmake/include")
     else ()
         # From Boost Package
-        find_package(Boost REQUIRED COMPONENTS capy corosio http json program_options scope system url)
+        find_package(Boost REQUIRED COMPONENTS capy corosio http json url)
         foreach (BOOST_INCLUDE_LIBRARY ${BOOST_INCLUDE_LIBRARIES})
             if (NOT TARGET Boost::${BOOST_INCLUDE_LIBRARY})
                 add_library(Boost::${BOOST_INCLUDE_LIBRARY} ALIAS Boost::headers)
@@ -150,7 +150,7 @@ source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR}/src PREFIX "src" FILES ${BOOST_BEA
 function(boost_beast2_setup_properties target)
     target_compile_features(${target} PUBLIC cxx_std_20)
     target_include_directories(${target}
-        PUBLIC "${PROJECT_SOURCE_DIR}/include"
+        PUBLIC $<BUILD_INTERFACE:${PROJECT_SOURCE_DIR}/include>
         PRIVATE "${PROJECT_SOURCE_DIR}")
     target_link_libraries(${target} PUBLIC ${BOOST_BEAST2_DEPENDENCIES})
     find_package(Threads REQUIRED)
@@ -180,6 +180,34 @@ add_library(boost_beast2 include/boost/beast2.hpp build/Jamfile ${BOOST_BEAST2_H
 add_library(Boost::beast2 ALIAS boost_beast2)
 boost_beast2_setup_properties(boost_beast2)
 
+include(GNUInstallDirs)
+include(CMakePackageConfigHelpers)
+set(BOOST_BEAST2_INSTALL_CMAKEDIR ${CMAKE_INSTALL_LIBDIR}/cmake/boost_beast2)
+set_target_properties(boost_beast2 PROPERTIES EXPORT_NAME beast2)
+target_include_directories(boost_beast2 PUBLIC $<INSTALL_INTERFACE:${CMAKE_INSTALL_INCLUDEDIR}>)
+install(TARGETS boost_beast2
+    EXPORT boost_beast2-targets
+    ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
+    LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
+    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR})
+install(DIRECTORY include/
+    DESTINATION ${CMAKE_INSTALL_INCLUDEDIR})
+install(EXPORT boost_beast2-targets
+    NAMESPACE Boost::
+    DESTINATION ${BOOST_BEAST2_INSTALL_CMAKEDIR})
+configure_package_config_file(
+    cmake/boost_beast2-config.cmake.in
+    ${CMAKE_CURRENT_BINARY_DIR}/boost_beast2-config.cmake
+    INSTALL_DESTINATION ${BOOST_BEAST2_INSTALL_CMAKEDIR})
+write_basic_package_version_file(
+    ${CMAKE_CURRENT_BINARY_DIR}/boost_beast2-config-version.cmake
+    VERSION ${PROJECT_VERSION}
+    COMPATIBILITY SameMajorVersion)
+install(FILES
+    ${CMAKE_CURRENT_BINARY_DIR}/boost_beast2-config.cmake
+    ${CMAKE_CURRENT_BINARY_DIR}/boost_beast2-config-version.cmake
+    DESTINATION ${BOOST_BEAST2_INSTALL_CMAKEDIR})
+
 #-------------------------------------------------
 #
 # Tests
--- /dev/null
+++ b/cmake/boost_beast2-config.cmake.in
@@ -0,0 +1,15 @@
+@PACKAGE_INIT@
+
+include(CMakeFindDependencyMacro)
+
+find_dependency(Threads)
+find_dependency(boost_headers)
+find_dependency(boost_capy)
+find_dependency(boost_corosio)
+find_dependency(boost_http)
+find_dependency(boost_json)
+find_dependency(boost_url)
+
+include("${CMAKE_CURRENT_LIST_DIR}/boost_beast2-targets.cmake")
+
+check_required_components(boost_beast2)
